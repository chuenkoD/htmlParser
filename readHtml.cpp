#include "header.h"
#include "parser.h"

#define BUF 1024 * 1024

int readTag(elHtml& p)
{
	char buf[BUF];

	while (fread(buf, sizeof(char), BUF, stdin))
	{

	}
	return 1;
}

int readAtr(elHtml& p)
{
	char buf[BUF];

	while (fread(buf, sizeof(char), BUF, stdin))
	{

	}
	return 1;
}